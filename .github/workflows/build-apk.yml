name: build-apk
on:
  push: { branches: [main] }
  workflow_dispatch:
jobs:
  apk:
    runs-on: ubuntu-latest
    env:
      RENPY_TAG: "8.5.3.26051504"
      RENPY_SHORT: "8.5.3"
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with: { distribution: temurin, java-version: '17' }
      - uses: actions/setup-python@v5
        with: { python-version: '3.12' }
      - name: SDL2 runtime libs
        run: |
          sudo apt-get update
          sudo apt-get install -y libsdl2-2.0-0 libsdl2-image-2.0-0 libsdl2-mixer-2.0-0 libsdl2-ttf-2.0-0
      - name: pip pygame_sdl2 (prebuilt wheel)
        run: |
          python -m pip install --upgrade pip
          pip install pygame_sdl2
          python -c "import pygame_sdl2; print('pygame_sdl2 OK', pygame_sdl2.__file__)"
      - name: Download official SDK
        run: |
          set -e
          F="renpy-${RENPY_SHORT}-sdk.tar.bz2"
          U1="https://github.com/renpy/renpy/releases/download/${RENPY_TAG}/${F}"
          U2="https://www.renpy.org/dl/${RENPY_SHORT}/${F}"
          wget -q "$U1" -O sdk.tar.bz2 || wget -q "$U2" -O sdk.tar.bz2
          TOP=$(tar tf sdk.tar.bz2 | head -1 | cut -d/ -f1)
          tar xf sdk.tar.bz2 && mv "$TOP" SDK
      - name: Ensure rapt
        run: |
          [ -d SDK/rapt ] || git clone --depth 1 https://github.com/renpy/rapt.git SDK/rapt
      - name: Inject + patches
        run: |
          cp android.json SDK/launcher/android.json
          bash patch.sh SDK/launcher
      - name: Verify rapt imports (system python)
        run: |
          cd SDK
          export PYTHONPATH="$(pwd)/rapt/buildlib"
          ( cd rapt && python -c "import pygame_sdl2, rapt.interface, rapt.install_sdk; print('ALL IMPORTS OK')" )
      - name: rapt installsdk
        run: |
          cd SDK
          export PYTHONPATH="$(pwd)/rapt/buildlib"
          ( cd rapt && yes | python android.py installsdk )
      - name: Build launcher DEBUG apk
        run: |
          cd SDK
          export PYTHONPATH="$(pwd)/rapt/buildlib"
          ( cd rapt && python android.py build launcher debug )
      - name: Collect apk
        run: |
          cd SDK && mkdir -p out
          find . -name '*.apk' -print -exec cp {} out/ \;
      - uses: actions/upload-artifact@v4
        with:
          name: launcher-apk
          path: SDK/out/*.apk
